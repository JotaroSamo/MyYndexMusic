<Page
    Background="Transparent"
    Height="450"
    Title="PlayList"
    Width="900"
    mc:Ignorable="d"
    x:Class="MyYndexMusic.Pages.PlayListPage.PlayList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:converters1="clr-namespace:MyYndexMusic.Core.Converters;assembly=MyYndexMusic.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MyYndexMusic.Pages.PlayListPage"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Page.Resources>
        <converters1:NullImageConverter x:Key="NullImageConverter" />

        <Style TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Margin" Value="10" />
            <Setter Property="BorderBrush" Value="LightGray" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style TargetType="ListView">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Page.Resources>

    <Grid Style="{StaticResource mainGridStyle}">
        <ListView Name="PlaylistsItemsControl">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Height="150" Width="150">
                        <StackPanel HorizontalAlignment="Center" Orientation="Vertical">
                            <Image
                                Height="60"
                                Margin="0,0,0,5"
                                Source="{Binding BackgroundImageUrl, Converter={StaticResource NullImageConverter}}"
                                Stretch="UniformToFill"
                                Width="60" />
                            <TextBlock
                                FontSize="12"
                                FontWeight="Bold"
                                HorizontalAlignment="Center"
                                Text="{Binding Title}"
                                TextWrapping="Wrap" />
                            <Button
                                Click="ButtonBase_OnClick"
                                Content="Старт"
                                HorizontalAlignment="Center"
                                Tag="{Binding Uid}"
                                Width="75" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>


